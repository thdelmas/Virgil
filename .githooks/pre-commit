#!/usr/bin/env bash
# Runs the same checks as CI, in fast mode.
# Bypass with `git commit --no-verify` only for WIP; fix and recommit before merging.
set -euo pipefail
exec "$(git rev-parse --show-toplevel)/scripts/code-quality-check.sh" fast
