[build]
  base    = "web"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — scoped to the subpath so only Virgil routes hit this.
[[redirects]]
  from   = "/apps/virgil/*"
  to     = "/apps/virgil/index.html"
  status = 200
